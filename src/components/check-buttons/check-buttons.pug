mixin cbuttons(uiText, textArr, checkedArr, font)
    .cbuttons
        .cbuttons__header
            +ui-text(uiText)
        .cbuttons__list
            +checkbox(textArr, checkedArr,0, font)
            